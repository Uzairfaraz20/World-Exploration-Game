import java.util.Scanner;

/**
 * Author: Vineet
 * Characters are not allowed to attack at all in a safe place
 */
public class SafePlace extends Place {
    public SafePlace(Scanner scanner, double version) {
        super(scanner, version);
    }
}
