import java.util.Scanner;

/**
 * Author: Vineet
 * If a AttackMove is done in an enhanced place, then the damage from the
 * attack is even greater
 */
public class EnhancedPlace extends Place {

    public EnhancedPlace(Scanner scanner, double version){
        super(scanner, version);
    }
}
